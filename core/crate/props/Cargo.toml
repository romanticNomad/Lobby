[package]
name = "props"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
alloy-primitives = {version = "1.5.2", features = ["serde"]}
k256 = { version = "0.13", features = ["ecdsa"] }
sha3 = "0.10"
