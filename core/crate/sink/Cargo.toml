[package]
name = "sink"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = [
    "rt-multi-thread",
    "macros",
    "sync",
    "time",
]}
kernel = { path = "../kernel" }
async-trait = "0.1.89"
