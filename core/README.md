# Lobby - core directory:
## bin
* `main` : entry point to lobby which wires down all the crates.
## crate
* `kernel` : foundational types and traits followed by lobby.
* `adapter` : eip1193 adapter for lobby.